name: Orchestrator Runtime
on:
  push:
    branches: ["orc-test"]
  workflow_dispatch:

jobs:
  run-orc-steps:
    runs-on: ubuntu-latest
    outputs:
      info: ${{ steps.run-orchestrator.outputs.info }}
      status: ${{ steps.run-orchestrator.outputs.status }}
    steps:
      - uses: actions/checkout@v4
      - name: Run Sample App
        id: run-orchestrator
        env:
          GITHUB_ACTIONS: "tue"
        run: |
          python $GITHUB_WORKSPACE/.github/workflows/sample_app.py pass
  run-orc-notify:
    needs: run-orc-steps
    uses: ./.github/workflows/orc_notifier.yml
    if: success() || failure()
    with:
      info: ${{ needs.run-orc-steps.outputs.info }}
      status: ${{ needs.run-orc-steps.outputs.status }}
        # secrets: inherit
        # secrets:
        #   envPAT: ${{ secrets.envPAT }}